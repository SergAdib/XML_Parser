# XML_Parser

## To run the test please clone into local repo and open with Visual Studio
## Build solution solition and then start without debugging (Ctrl + F5)
## That will run both Web API & UI projects, with UI on port 8080 (please make sure port is free or update CORS settings in builder config if browser opens with alternative port)

##
##
## P.S. I haven't removed artefacts from WebAPI Project creation since it's just a test, so weather controller still there...
